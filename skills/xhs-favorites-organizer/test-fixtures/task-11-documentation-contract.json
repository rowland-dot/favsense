{
  "product_contracts": {
    "skills/xhs-favorites-organizer/SKILL.md": {
      "required_phrases": [
        "点点原子保存后是 `captured`，证据或资源不完整时是 `pending`，只有当前正文/证据 revision 与完整审计相符时才是 `accepted`。",
        "真正尝试失败的当前笔记写 `failed`，尚未尝试的剩余笔记写 `batch_aborted`；二者不得共用失败语义。",
        "关键词只能产生 `candidateKind=\"Skill\"`；公开 `confirmed Skill` 必须是 accepted，并且恰好关联一个当前、完整、已核验的官方资源",
        "最后一个收藏夹完成只形成核心 checkpoint；Bridge 必须等待冻结范围内的总结、证据、候选、资源与 curation 状态确定",
        "两个 staging 输出全部验证并原子交换成功后，才允许调用 `publish-huggingface.mjs`，同一运行最多发布一次。",
        "Spec → Plan → TDD → Review → QA → Audit → Brief → 用户批准 → PR"
      ],
      "forbidden_phrases": [
        "每个面板最多发布一次",
        "最后一个收藏夹完成后立即发布",
        "`captured` 等于 `accepted`",
        "`failed` 与 `batch_aborted` 含义相同"
      ]
    },
    "skills/xhs-favorites-organizer/references/automatic-workflow.md": {
      "required_phrases": [
        "每篇成功原子保存为 captured；真实失败为 failed，未尝试剩余项为 batch_aborted",
        "缺证据保持 pending，只有完整当前记录进入 accepted",
        "关键词候选只输出 candidateKind；confirmed Skill 必须恰好关联一个 verified resource",
        "两份 staging 全部验证并交换成功后，按配置最多发布一次到 Hugging Face Static Space",
        "`captured` 只表示点点回复已按当前正文版本安全保存；`pending` 表示还缺审核、证据或资源；`accepted` 才能进入正式知识库和公开页面。"
      ],
      "forbidden_phrases": [
        "每个面板最多发布一次",
        "最后一个收藏夹完成后立即发布",
        "failed 和 batch_aborted 是同一状态"
      ]
    },
    "skills/xhs-favorites-organizer/references/curation-standard.md": {
      "required_phrases": [
        "点点记录成功原子保存后为 `captured`，不等于 `accepted`。当前真实尝试失败为 `failed`；因停批而未尝试的其余项为 `batch_aborted`。",
        "确定性分类只能留下 `candidateKind=\"Skill\"`；公开 `confirmed Skill` 还必须通过 accepted/current/resource 门。",
        "迁移默认 dry-run，只生成不含笔记 ID、标题或绝对路径的计数报告",
        "只有最终快照成功后才允许最多发布一次。"
      ],
      "forbidden_phrases": [
        "raw 点点回复可以直接发布",
        "允许对真实收藏数据执行 `--apply`"
      ]
    },
    "skills/xhs-favorites-organizer/references/organization-schema.md": {
      "required_phrases": [
        "规则判断为 Skill 但未满足 accepted/current/resource 门时，正式 `kind` 使用领域中性 fallback，并额外输出 `candidateKind=\"Skill\"`；只有 accepted 且恰好关联一个 fresh verified resource 时才是 `confirmed Skill`。",
        "`resource_identity_sha256`：绑定 canonical repo、官方名称/类型、license、manifest、compatibility 与官方证据位置；",
        "`verification_snapshot_sha256`：绑定 identity、默认分支、下载地址、Star 与核验日期；",
        "confirmed Skill 的资源动作必须至少包含名称可区分的“官方仓库”和“下载 ZIP”"
      ],
      "forbidden_phrases": [
        "找不到唯一官方来源时生成猜测链接"
      ]
    },
    "README.md": {
      "required_phrases": [
        "核心目录保存完成时先显示“核心收藏已保存”；点点回复原子保存后是 `captured`，缺证据、资源或语义审核时保持 `pending`，只有当前 revision 与质量门全部匹配时才是 `accepted`。",
        "本机详情可以通过鉴权的“待审核证据” overlay 查看清理后的 captured 内容，公开页面和正式知识库不会直接采用 raw 点点回复。",
        "关键词只会产生 `candidateKind=\"Skill\"`。公开 `confirmed Skill` 必须是 accepted，并恰好关联一个完整、当前、已核验的官方资源",
        "最后一个收藏夹完成只建立核心 checkpoint；curation 完成后才构建一个共享版本的知识库与网页快照，验证成功后按配置最多发布一次。"
      ],
      "forbidden_phrases": [
        "最后一个收藏夹完成后立即发布",
        "公开页面直接采用 raw 点点回复"
      ]
    },
    "docs/ARCHITECTURE.md": {
      "required_phrases": [
        "点点或离线证据先形成 candidate；资源评估与 audit 明确产生 pending/rejected/accepted",
        "两个正式构建器共用同一 acceptance decision，在 staging 中生成相同 `build_version`",
        "发布只在最终快照成功后按配置执行，单个 run 最多一次",
        "`candidateKind=Skill` 只有在 accepted 且恰好一个 fresh verified resource 时才提升为 confirmed Skill。"
      ],
      "forbidden_phrases": [
        "每个面板最多发布一次"
      ]
    },
    "docs/PUBLISHING.md": {
      "required_phrases": [
        "只有通过 current curation 与 accepted audit 的正式摘要可以进入；captured/pending 证据只留在本机 overlay。",
        "curation 质量门通过后，双输出协调器构建共享 `build_version`",
        "同一运行最多发布一次；",
        "构建失败设置 `held_previous` 并保留同一上一版；",
        "没有内容变化时，发布器返回 `unchanged`，不会产生空提交。",
        "发布前不会因“最后一个收藏夹完成”而提前构建或发布。"
      ],
      "forbidden_phrases": [
        "每个面板最多发布一次",
        "captured/pending 证据可以公开"
      ]
    }
  },
  "developer_contracts": {
    "CONTRIBUTING.md": {
      "required_phrases": [
        "本项目统一要求 Node.js 20 与 Python 3.12。",
        "首次安装依赖后还需安装 Playwright Chromium：Windows 运行 `npx.cmd playwright install chromium`；Ubuntu/CI 运行 `npx playwright install --with-deps chromium`。",
        "浏览器脚本 | `npm.cmd run check:syntax`、`npm.cmd run lint:a11y`、`npm.cmd run test:site` | `npm.cmd run test:e2e`、`npm.cmd run release:check`",
        "合成 QA 服务的场景和 UX/VC 映射可由 `npm.cmd run preview:qa-fixture -- --help` 查看",
        "迁移测试只能使用 synthetic fixture 和默认 dry-run。真实迁移 apply 必须在 review brief 获用户批准后另行授权",
        "Spec → Plan → TDD → Review → QA → Audit → Brief → 用户批准 → PR"
      ],
      "forbidden_phrases": [
        "真实迁移 apply 属于自动测试范围"
      ]
    }
  },
  "package_script_contracts": {
    "windows_contract_fragments": [
      "test_setup_browser_profile.ps1",
      "test_setup_transaction.ps1",
      "test_setup_xhs_downloader.ps1",
      "test_start_autosync.ps1",
      "-ScriptPath skills/xhs-favorites-organizer/scripts/setup-autosync.ps1",
      "-ScriptPath skills/xhs-favorites-organizer/scripts/setup-xhs-downloader.ps1",
      "-ScriptPath \"",
      "skills/xhs-favorites-organizer/scripts/start-autosync.ps1"
    ],
    "release_gate_commands": [
      "npm run lint:a11y",
      "npm run test:organization",
      "npm run test:e2e"
    ]
  }
}
