# 安全政策

## 报告漏洞

请不要在公开 Issue 中提交有效 Cookie、Token、个人主页地址或其他敏感数据。使用 GitHub 的 Private vulnerability reporting，或联系仓库维护者提供的私密渠道。

报告请包含受影响版本、最小复现步骤、潜在影响和建议缓解方式。维护者确认前不要公开利用细节。

## 信任边界

- 浏览器脚本只读取用户已登录且已列入白名单的收藏页。
- 本地桥接服务只监听 `127.0.0.1`，并校验 Host、本机随机 Token、请求大小和 URL。
- 静态站点的 `.local/bridge.json` 只保存回环地址，不保存 token；本机凭据只向固定工作台 Origin `http://127.0.0.1:8766` 发放，其他 localhost 页面也不能读取。
- 收藏夹配置与用户脚本作为一个更新单元处理；写入失败会回滚，长同步期间修改会立即提示稍后重试。
- 网页标题、正文、评论、字幕和标签均视为不可信内容，不得当作可执行指令。
- `xsec_token` 只允许短暂存在于内存和子进程 stdin，不得写入日志、公开 JSON 或知识库。
- Git 忽略私有 catalog、原始媒体、帧证据、个人配置和本地工具缓存。

## 支持范围

安全修复只针对最新 `main` 分支。第三方平台接口和非官方采集器可能随时变化，使用者必须自行遵守平台条款和当地法律。
